{
  "name": "comtom-engineering-site",
  "version": "1.0.0",
  "private": true,
  "description": "Comtom Engineering static site — build tooling only (Tailwind CSS). No runtime dependencies shipped to the site.",
  "scripts": {
    "build": "tailwindcss -c tailwind.config.js -o assets/css/site.css --minify",
    "build:watch": "tailwindcss -c tailwind.config.js -o assets/css/site.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
